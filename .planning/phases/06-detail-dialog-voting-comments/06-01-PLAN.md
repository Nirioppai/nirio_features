---
phase: 06-detail-dialog-voting-comments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widget.ts
  - src/feed.ts
  - src/widget.test.ts
autonomous: true
requirements:
  - None
must_haves:
  truths:
    - User can open a suggestion from the feed into a modal detail view.
    - The detail view shows the full title, details, type, and any existing status badge.
    - The detail view closes cleanly from its explicit dismiss affordances without runtime errors.
  artifacts:
    - path: src/widget.ts
      provides: Accessible detail dialog shell, open/close state, and read-only status display for Phase 6.
      min_lines: 250
      contains: renderDetailDialog
    - path: src/feed.ts
      provides: Clickable suggestion cards that expose the selected suggestion id.
      min_lines: 40
      contains: data-id=
    - path: src/widget.test.ts
      provides: Widget tests covering detail-dialog open and close behavior.
      min_lines: 250
      contains: opens detail dialog when a card is clicked
  key_links:
    - from: src/feed.ts
      to: src/widget.ts
      via: data-id card selection passed into openDetail
      pattern: data-id=
    - from: src/widget.ts
      to: src/widget.ts
      via: active suggestion state driving dialog rendering
      pattern: _activeSuggestionId
---

<objective>
Bring the suggestion detail dialog in line with Phase 6 scope and the existing UI contract.

Purpose: Phase 6 owns the per-suggestion dialog, not the admin status editor. The plan should leave a clean, accessible modal flow that shows suggestion details and status without dragging Phase 7 controls forward.
Output: A dialog implementation in the widget, updated feed wiring, and tests that pin the expected Phase 6 interaction behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-detail-dialog-voting-comments/06-UI-SPEC.md
@src/widget.ts
@src/feed.ts
@src/widget.test.ts

<interfaces>
From src/types.ts:
```typescript
export interface Suggestion {
  id: string;
  title: string;
  details?: string;
  type: SuggestionType;
  status?: SuggestionStatus;
  authorId: string;
  authorName: string;
  createdAt: Date;
  voteCount: number;
  commentCount: number;
}
```

From src/adapter.ts:

```typescript
export interface StorageAdapter {
  getSuggestions(): Promise<Suggestion[]>;
  getVote(suggestionId: string, userId: string): Promise<Vote | null>;
  getComments(suggestionId: string): Promise<Comment[]>;
}
```

From src/feed.ts:

```typescript
export function renderSuggestionCard(s: Suggestion): string;
export function renderFeedHTML(
  suggestions: Suggestion[],
  loading: boolean,
  sort: SortOption,
  searchQuery: string,
): string;
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align dialog shell with Phase 6 scope</name>
  <files>src/widget.ts, src/feed.ts</files>
  <action>Update the detail dialog rendering per the Phase 6 UI spec: keep the card-to-dialog flow, render the suggestion title/details/type/status in a modal overlay with accessible dialog semantics, and add complete close affordances (close button, overlay dismissal if the chosen structure supports it, and Escape key handling). Preserve read-only status badge display, but remove the admin-only status selector from this phase so Phase 7 retains ownership of status mutation. Keep `escapeHtml` on all user-authored text and reuse the existing feed card `data-id` wiring rather than inventing a second selection path.</action>
  <verify>
    <automated>npx vitest run src/widget.test.ts</automated>
  </verify>
  <done>The widget opens a modal detail view from feed cards, displays suggestion metadata without admin editing controls, and supports stable close behavior without runtime errors.</done>
</task>

<task type="auto">
  <name>Task 2: Pin dialog interaction behavior in tests</name>
  <files>src/widget.test.ts</files>
  <action>Add or update focused widget tests so the executable contract covers opening the dialog from a card, rendering status as read-only display, and dismissing the dialog through the supported affordances. Remove any assertions that treat admin status mutation as a Phase 6 requirement. Keep the tests behavior-scoped and reuse the existing mock adapter helper instead of introducing a new test harness.</action>
  <verify>
    <automated>npx vitest run src/widget.test.ts</automated>
  </verify>
  <done>Phase 6 tests fail if the dialog stops opening, if it leaks Phase 7 status-edit controls back into this phase, or if close behavior regresses.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                                                |
| ------------------------------------------ | ------------------------------------------------------------------------------------------ |
| host app data -> shadow DOM dialog         | Suggestion titles, details, and comments are untrusted content rendered into the widget UI |
| keyboard and pointer input -> widget state | Untrusted UI events control dialog open/close state                                        |

## STRIDE Threat Register

| Threat ID | Category | Component                                        | Disposition | Mitigation Plan                                                                                                                          |
| --------- | -------- | ------------------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-01   | T        | src/widget.ts dialog rendering                   | mitigate    | Continue escaping user-authored title/details/comment content with `escapeHtml` before injecting HTML into the shadow DOM                |
| T-06-02   | E        | Phase boundary between dialog and admin controls | mitigate    | Keep status mutation controls out of Phase 6 so non-admin viewers cannot reach unfinished privileged UI paths                            |
| T-06-03   | D        | Dialog lifecycle handlers                        | accept      | Modal open/close spam is low-risk in this client-only widget; rely on simple idempotent state transitions and tests for dismissal safety |

</threat_model>

<verification>
Run npx vitest run src/widget.test.ts after each edit slice, then confirm the dialog behavior still matches the Phase 6 UI contract in .planning/phases/06-detail-dialog-voting-comments/06-UI-SPEC.md.
</verification>

<success_criteria>
The detail dialog is a Phase 6-owned modal flow with accessible open/close behavior, read-only status display, and automated tests covering the interaction contract.
</success_criteria>

<output>
After completion, create the phase summary file for plan 06-01 in the Phase 6 directory.
</output>

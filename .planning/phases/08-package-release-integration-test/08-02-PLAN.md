---
phase: 08-package-release-integration-test
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - .github/workflows/publish.yml
  - README.md
autonomous: true
requirements:
  - None
user_setup:
  - service: github-packages
    why: GitHub Packages install and publish verification requires package registry authentication outside the repository codebase.
    env_vars:
      - name: NODE_AUTH_TOKEN
        source: GitHub personal access token with read:packages for install testing and write:packages for manual publish fallback
    dashboard_config:
      - task: Ensure Actions and Packages permissions allow publishing from this repository
        location: GitHub repository settings and package permissions
must_haves:
  truths:
    - The publish workflow refuses to publish unless the Phase 8 release verification command succeeds on the tagged build.
    - The README shows a consumer exactly how to configure `.npmrc`, install from GitHub Packages, register the widget, and pass Firebase adapter plus user props without guessing hidden setup.
    - The release instructions describe the tag-driven GitHub Actions publish path for v1 releases instead of relying only on local manual publish commands.
  artifacts:
    - path: .github/workflows/publish.yml
      provides: Tag-triggered GitHub Packages publish workflow guarded by the Phase 8 release verification command.
      min_lines: 20
      contains: test:phase8
    - path: README.md
      provides: Install, usage, and release documentation aligned with GitHub Packages auth and the shipped widget API.
      min_lines: 90
      contains: .npmrc
  key_links:
    - from: .github/workflows/publish.yml
      to: package.json
      via: workflow runs the dedicated Phase 8 release verification script before npm publish
      pattern: test:phase8
    - from: README.md
      to: src/index.ts
      via: docs use the public exports a consumer installs from the package
      pattern: defineWidget|createFirebaseAdapter
---

<objective>
Lock the release pipeline and consumer documentation to the real package contract established by Phase 8 verification.

Purpose: Phase 8 is only complete when publish automation and installation guidance reflect the actual release path. The package already builds locally; this plan makes the GitHub Packages workflow prove the release slice before publishing and documents the exact install and tag-based release steps a consumer or maintainer must follow.
Output: A publish workflow guarded by Phase 8 verification and a README that matches GitHub Packages installation, widget initialization, and release mechanics.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@package.json
@.github/workflows/publish.yml
@README.md
@src/index.ts
@src/adapters/firebase.ts

<interfaces>
From package.json:
```json
{
  "name": "@nirioppai/feature-suggestions",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "build": "tsup"
  }
}
```

From src/index.ts:

```typescript
export { createFirebaseAdapter } from './adapters/firebase';
export { defineWidget } from './widget';
export type { WidgetUser, WidgetTheme, SortOption } from './widget';
```

From src/adapters/firebase.ts:

```typescript
export function createFirebaseAdapter(firestore: Firestore): StorageAdapter;
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard GitHub Packages publishing with the Phase 8 release verification command</name>
  <files>.github/workflows/publish.yml</files>
  <action>Update `.github/workflows/publish.yml` so the tag-triggered publish job runs the dedicated `npm run test:phase8` command from plan 08-01 before any `npm publish` step. Keep the existing GitHub Packages registry configuration and tag trigger, but make the workflow fail before publish if the release verification slice fails. Avoid redundant ad hoc commands that can drift from `package.json`; the workflow should delegate release proof to the script contract rather than duplicating consumer-test and pack logic inline.</action>
  <verify>
    <automated>npm run test:phase8</automated>
  </verify>
  <done>The publish workflow cannot ship a broken package because it runs the same Phase 8 release command that was validated locally before publishing.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite install and release docs around the actual GitHub Packages consumer flow</name>
  <files>README.md</files>
  <action>Expand `README.md` so a consumer can install the private package without tribal knowledge. Add a `.npmrc` example for the `@nirioppai` scope and token-based GitHub Packages auth, keep the install command aligned to the published package name, and provide a usage example that calls `defineWidget()`, creates a Firebase adapter, and sets `user` plus `adapter` on the custom element. Update the release section to document the tag-based GitHub Actions path for shipping versions, including version bump, tag push, and the expectation that the workflow runs Phase 8 verification before publish. Remove or rewrite any wording that implies local `npm publish` is the primary release path.</action>
  <verify>
    <automated>npm run test:phase8 && npm pack --dry-run</automated>
  </verify>
  <done>The README accurately documents private-package installation, consumer initialization, and the tag-driven GitHub Packages release flow that matches the repository automation.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                              |
| ----------------------------------------- | ---------------------------------------------------------------------------------------- |
| git tag -> GitHub Actions publish job     | Tagged releases cross from source control intent into package publication                |
| package docs -> consumer setup            | Consumers rely on README instructions for registry auth and initialization wiring        |
| GitHub Packages auth -> release execution | Missing or incorrect package permissions can cause failed or misleading release attempts |

## STRIDE Threat Register

| Threat ID | Category | Component                     | Disposition | Mitigation Plan                                                                                                                                |
| --------- | -------- | ----------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| T-08-04   | T        | .github/workflows/publish.yml | mitigate    | Require `npm run test:phase8` before `npm publish` so tags cannot publish unverified artifacts                                                 |
| T-08-05   | I        | README.md install docs        | mitigate    | Document scoped `.npmrc` auth and the exact package name so consumers do not leak setup assumptions into trial-and-error commands              |
| T-08-06   | D        | GitHub Packages release auth  | mitigate    | Capture `NODE_AUTH_TOKEN` and repository package-permission prerequisites in `user_setup` so release auth gaps are explicit rather than latent |

</threat_model>

<verification>
Run `npm run test:phase8 && npm pack --dry-run` after updating the workflow and README. Manually inspect `.github/workflows/publish.yml` to confirm the publish job delegates release proof to `npm run test:phase8` before `npm publish`.
</verification>

<success_criteria>
The repository documents and automates one coherent GitHub Packages release path: consumers know how to authenticate and install the package, and maintainers know that tag-based publishing is blocked until Phase 8 verification passes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-package-release-integration-test/08-02-SUMMARY.md`.
</output>

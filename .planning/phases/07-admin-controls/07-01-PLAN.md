---
phase: 07-admin-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/status.ts
  - src/status.test.ts
  - src/feed.ts
  - src/feed.test.ts
autonomous: true
requirements:
  - None
must_haves:
  truths:
    - All users see a status badge on suggestion cards whenever a suggestion has a stored status.
    - Status badges use the exact Phase 7 status vocabulary and a deterministic semantic class for styling.
    - Suggestions without a status keep the existing badge-free card layout.
  artifacts:
    - path: src/status.ts
      provides: Canonical Phase 7 status metadata and badge class helpers shared by feed and dialog UI.
      min_lines: 30
      contains: statusToClassName
    - path: src/feed.ts
      provides: Feed card badge rendering wired to the shared status helper.
      min_lines: 60
      contains: renderSuggestionCard
    - path: src/feed.test.ts
      provides: Feed-level coverage for status badge visibility and semantic class output.
      min_lines: 140
      contains: renders status badge when status is set
  key_links:
    - from: src/status.ts
      to: src/feed.ts
      via: shared badge class helper used by renderSuggestionCard
      pattern: statusToClassName
    - from: src/types.ts
      to: src/status.ts
      via: SuggestionStatus union constrains the shared Phase 7 status vocabulary
      pattern: SuggestionStatus
---

<objective>
Establish the shared status presentation contract for Phase 7 before the admin editor is wired.

Purpose: Phase 6 already renders read-only status text, but Phase 7 needs a single source of truth for the five allowed statuses, their semantic CSS modifier slugs, and feed-card badge behavior so the admin flow does not duplicate or drift badge logic across surfaces.
Output: A reusable status helper module plus feed rendering and tests that prove badge visibility and semantic class generation for Phase 7 statuses.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-admin-controls/07-UI-SPEC.md
@.planning/phases/06-detail-dialog-voting-comments/06-01-SUMMARY.md
@src/types.ts
@src/feed.ts
@src/feed.test.ts

<interfaces>
From src/types.ts:
```typescript
export type SuggestionStatus =
  | 'Under Review'
  | 'Planned'
  | 'In Progress'
  | 'Completed'
  | 'Declined';

export interface Suggestion {
id: string;
title: string;
details?: string;
type: SuggestionType;
status?: SuggestionStatus;
authorId: string;
authorName: string;
createdAt: Date;
voteCount: number;
commentCount: number;
}

````

From src/feed.ts:
```typescript
export function renderSuggestionCard(s: Suggestion): string;
export function renderFeedHTML(
  suggestions: Suggestion[],
  loading: boolean,
  sort: SortOption,
  searchQuery: string,
): string;
````

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the Phase 7 status presentation helper</name>
  <files>src/status.ts, src/status.test.ts</files>
  <behavior>
    - Test 1: Each allowed status maps to a stable slug used in CSS modifier classes.
    - Test 2: Unknown or empty input cannot inject arbitrary class names into badge markup.
    - Test 3: The exported status options preserve the exact UI-SPEC order: Under Review, Planned, In Progress, Completed, Declined.
  </behavior>
  <action>Create a small shared module for Phase 7 status presentation. Export the ordered status options used by the admin selector, a helper that converts a status into the exact modifier slug required by the UI spec (`under-review`, `planned`, `in-progress`, `completed`, `declined`), and any minimal badge metadata needed by feed and dialog rendering. Do not move storage logic into this module and do not introduce framework-specific abstractions. Keep the helper deterministic so later widget rendering can consume it without rebuilding status logic in multiple places.</action>
  <verify>
    <automated>npx vitest run src/status.test.ts</automated>
  </verify>
  <done>The codebase has one canonical Phase 7 status helper with executable tests covering option order and class-slug generation.</done>
</task>

<task type="auto">
  <name>Task 2: Wire feed cards to the shared status contract</name>
  <files>src/feed.ts, src/feed.test.ts</files>
  <action>Update feed card rendering to consume the shared status helper instead of hardcoding a generic `fs-card-status` badge. Keep the badge optional when `suggestion.status` is absent, and when present render the exact stored status text plus the base badge class and semantic modifier class required by the Phase 7 UI spec. Expand feed tests to prove the badge appears only when status exists and that the rendered HTML includes the expected semantic class for at least one multi-word status such as `In Progress`.</action>
  <verify>
    <automated>npx vitest run src/status.test.ts src/feed.test.ts</automated>
  </verify>
  <done>Feed cards expose Phase 7 status badges with stable semantic classes and tests fail if the badge text or class contract regresses.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                        |
| ---------------------------------------- | ---------------------------------------------------------------------------------- |
| persisted suggestion status -> feed HTML | Status values loaded from storage are rendered into badge markup in the shadow DOM |
| status helper -> CSS selector space      | Helper output controls class-name fragments used for semantic badge styling        |

## STRIDE Threat Register

| Threat ID | Category | Component                           | Disposition | Mitigation Plan                                                                                                                   |
| --------- | -------- | ----------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------- |
| T-07-01   | T        | src/status.ts class-name generation | mitigate    | Map only the five allowed statuses to fixed slugs so stored data cannot inject arbitrary CSS classes into badge markup            |
| T-07-02   | I        | src/feed.ts badge rendering         | accept      | Status values are product metadata rather than sensitive data, so rendering them to all users is expected Phase 7 behavior        |
| T-07-03   | D        | src/feed.ts optional badge layout   | mitigate    | Keep badge rendering conditional on `suggestion.status` so cards without statuses do not break layout or render placeholder noise |

</threat_model>

<verification>
Run `npx vitest run src/status.test.ts src/feed.test.ts` after each edit slice. Confirm the rendered feed badge classes align with the Phase 7 UI spec color map and naming contract before moving on to widget-level admin controls.
</verification>

<success_criteria>
The codebase has a single shared Phase 7 status contract, and feed cards already render all-user badges with the exact status text and semantic class names the admin dialog will reuse.
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-controls/07-01-SUMMARY.md`.
</output>

---
phase: 07-admin-controls
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/widget.ts
  - src/widget.test.ts
  - package.json
autonomous: true
requirements:
  - None
must_haves:
  truths:
    - Admin users see an `Update Status` selector inside the detail dialog and non-admin users do not.
    - Changing the selector persists through `StorageAdapter.setStatus()` and updates both the open dialog and the feed card without a page reload.
    - Failed status saves restore the prior selected value and show the inline error copy from the Phase 7 UI spec.
    - All users still see the read-only status badge in the detail dialog after the admin controls are added.
  artifacts:
    - path: src/widget.ts
      provides: Admin-only status select flow, in-flight disabled state, rollback-on-error handling, and dialog badge rendering using the shared Phase 7 status helper.
      min_lines: 350
      contains: fs-status-select
    - path: src/widget.test.ts
      provides: Widget interaction tests for admin visibility, status persistence, and error rollback behavior.
      min_lines: 520
      contains: setStatus
    - path: package.json
      provides: Reproducible Phase 7 verification entry point.
      min_lines: 20
      contains: test:phase7
  key_links:
    - from: src/widget.ts
      to: src/adapter.ts
      via: change handler calls setStatus with the selected suggestion id and status
      pattern: setStatus
    - from: src/widget.ts
      to: src/feed.ts
      via: local suggestion status mutation followed by renderFeed and renderDetailDialog
      pattern: renderFeed\(|renderDetailDialog\(
    - from: package.json
      to: src/widget.test.ts
      via: phase-specific verification script runs the focused admin-controls test slice
      pattern: test:phase7
---

<objective>
Add the admin-only status editor to the detail dialog and verify the end-to-end Phase 7 interaction contract.

Purpose: The storage layer already supports status persistence, and Phase 6 intentionally left status read-only. This plan closes the remaining Phase 7 gap by making status mutation available only to admins, keeping status badges visible to everyone, and pinning the success and rollback paths in widget tests.
Output: Admin status editing in the widget dialog, focused regression coverage, and a repeatable phase-specific verification script.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-admin-controls/07-UI-SPEC.md
@.planning/phases/06-detail-dialog-voting-comments/06-01-SUMMARY.md
@.planning/phases/06-detail-dialog-voting-comments/06-02-SUMMARY.md
@src/widget.ts
@src/widget.test.ts
@src/adapter.ts
@src/types.ts
@package.json

<interfaces>
From src/adapter.ts:
```typescript
export interface StorageAdapter {
  getSuggestions(): Promise<Suggestion[]>;
  setStatus(suggestionId: string, status: SuggestionStatus): Promise<void>;
  getComments(suggestionId: string): Promise<Comment[]>;
  getVote(suggestionId: string, userId: string): Promise<Vote | null>;
}
```

From src/widget.ts:

```typescript
export interface WidgetUser {
  id: string;
  name: string;
  email: string;
  role: string;
}
```

From plan 07-01 output (`src/status.ts`):

```typescript
export const STATUS_OPTIONS: SuggestionStatus[];
export function statusToClassName(status: SuggestionStatus): string;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the admin-only status selector into the detail dialog</name>
  <files>src/widget.ts</files>
  <behavior>
    - Test 1: Admin users see the `Update Status` label and selector; non-admin users do not.
    - Test 2: Selecting a new status disables the selector while `setStatus()` is in flight, then updates the dialog and feed badge on success.
    - Test 3: If `setStatus()` rejects, the selector returns to its previous value and the inline UI-SPEC error copy is rendered.
  </behavior>
  <action>Extend the detail dialog in `src/widget.ts` to match the Phase 7 UI spec. Use the shared status helper from plan 07-01 for option order and badge class names. Render the admin section only when `this._user?.role === 'admin'`, place it between the badge row and details copy, and keep the existing read-only badge visible for all viewers. On select change, call `this._adapter.setStatus(activeSuggestionId, nextStatus)`, disable the selector while the promise is pending, update the in-memory suggestion status only after success, then re-render both feed and dialog. On failure, restore the prior selection, re-enable the control, and show `Couldn't save status. Try again.` inline without swallowing other dialog behavior. Do not add a separate save button, confirmation modal, or page reload.</action>
  <verify>
    <automated>MISSING — Wave 2 must create or update widget tests in src/widget.test.ts first</automated>
  </verify>
  <done>The widget exposes admin-only status editing with immediate persistence, disabled-in-flight behavior, and rollback-on-error while preserving all-user badge visibility.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the Phase 7 interaction contract in tests and scripts</name>
  <files>src/widget.test.ts, package.json</files>
  <action>Replace the current read-only Phase 6 admin assertions with focused Phase 7 coverage. Add tests for admin-only visibility, non-admin badge-only behavior, successful status changes calling `setStatus()` and updating the rendered badge text, and failed status updates restoring the prior selected value plus inline error copy. Keep the existing `makeMockAdapter()` helper and the build-aware test pattern established in Phase 6. Add a dedicated `test:phase7` script that runs the focused Phase 7 slice reproducibly from a clean checkout; it should include a build step before any dist-based smoke coverage, not rely on ambient build artifacts.</action>
  <verify>
    <automated>npm run test:phase7</automated>
  </verify>
  <done>Phase 7 has a single reproducible verification command, and widget tests fail if admin visibility, persistence, or rollback behavior regresses.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                                           |
| ------------------------------------------ | ------------------------------------------------------------------------------------- |
| host app user role -> privileged widget UI | The host application controls whether the widget treats the current user as an admin  |
| widget dialog -> storage adapter           | Admin status changes mutate persisted suggestion records through the adapter contract |
| storage failure -> local widget state      | Rejected writes can desynchronize rendered status unless rollback is explicit         |

## STRIDE Threat Register

| Threat ID | Category | Component                                     | Disposition | Mitigation Plan                                                                                                          |
| --------- | -------- | --------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------ |
| T-07-04   | E        | src/widget.ts admin controls                  | mitigate    | Gate status selector rendering on `this._user?.role === 'admin'` and keep non-admin flows badge-only                     |
| T-07-05   | T        | src/widget.ts status persistence path         | mitigate    | Update in-memory suggestion status only after `setStatus()` resolves and roll back UI state on rejection                 |
| T-07-06   | R        | src/widget.test.ts admin interaction coverage | mitigate    | Add explicit success and failure-path tests so status mutations remain observable and regressions are detectable         |
| T-07-07   | D        | package.json phase verification               | mitigate    | Provide a dedicated `test:phase7` script so executors can prove behavior without ad hoc commands or hidden prerequisites |

</threat_model>

<verification>
Run `npm run test:phase7` after implementing the widget changes. Before declaring success, confirm the selector label, default option, inline error copy, and status badge ordering match `.planning/phases/07-admin-controls/07-UI-SPEC.md`.
</verification>

<success_criteria>
Admin users can update suggestion status from the detail dialog with immediate persistence and rollback safety, while all users continue to see the correct status badge on both feed cards and the dialog.
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-controls/07-02-SUMMARY.md`.
</output>
